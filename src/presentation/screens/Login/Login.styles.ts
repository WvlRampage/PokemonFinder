import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    bgContainer:{
      flex: 1,
    },
    container: {
      flex: 2.7,
      padding: 16,
    },
    titleContainer:{
      flex:1,
      marginTop:20,
    },
    title: {
      fontSize: 24,
      fontWeight: 'bold',
      marginBottom: 16,
      textAlign: 'center',
    },
    input: {
      borderWidth: 1,
      borderColor: '#ccc',
      padding: 8,
      marginBottom: 16,
      borderRadius: 4,
    },
});